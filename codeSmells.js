function getFullName1(firstName, lastName) {
  return firstName + ' ' + lastName;
}

function getFullName2(firstName, lastName) {
  return firstName + ' ' + lastName; // Same as getFullName1
}


function processOrder(order) {
  // 50 lines of logic here for handling orders...
} // Too long, can be broken into smaller functions


function createUser(firstName, lastName, email, address, city, state, zip, phone) {
  // Too many parameters
}
